{
    "name": "js-tests",
    "version": "1.0.0",
    "description": "Testing environment using Mocha and Chai with XML output",
    "scripts": {
      "test": "mocha code.js --reporter mocha-junit-reporter --reporter-options mochaFile=/LocalVolume/testresults.xml"
    },
    "devDependencies": {
      "chai": "^4.3.4",
      "mocha": "^9.1.1",
      "mocha-junit-reporter": "^2.0.0"
    }
  }
  