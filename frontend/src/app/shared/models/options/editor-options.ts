export interface EditorOptions {
    language: string;
    theme: string;
    minimap: {
        enabled: boolean;
    };
    automaticLayout: boolean;
    useShadows: boolean;
    wordWrap: string;
    lineNumbers: string;
}
